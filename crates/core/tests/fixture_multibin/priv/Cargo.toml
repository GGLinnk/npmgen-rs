# A private crate: `publish = false`. npmgen skips it like cargo does, unless it
# is named explicitly with --package.
[package]
name = "priv"
version = "1.0.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/acme/priv"
authors = ["Acme <dev@acme.test>"]
publish = false
